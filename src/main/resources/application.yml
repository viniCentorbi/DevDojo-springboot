server:
  error:
    include-stacktrace: on_param

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/DevDojo?createDatabaseIfNotExist=true
    username: root
    password: 123mysql
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update